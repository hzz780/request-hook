import { useFetch } from './fetch';
import { useSocket } from './socket-io';
import { useGraphql } from './graphql';

export { useGraphql, useSocket, useFetch };
export default { useGraphql, useSocket, useFetch };
